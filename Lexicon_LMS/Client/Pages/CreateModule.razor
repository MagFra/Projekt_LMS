@page "/createmodule/{CourseId:int}"
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize]

<AddModule CourseId="@CourseId"></AddModule>

@code{
    [Parameter]
    public int CourseId { get; set; }
}